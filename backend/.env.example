# Server configuration
PORT=5000
NODE_ENV=development
FRONTEND_ORIGIN=http://localhost:3000

# Authentication
JWT_SECRET=change-me

# MongoDB configuration
# Replace with your connection string. Paste the URI only (without an extra "MONGO_URI=" prefix).
MONGO_URI=mongodb://localhost:27017/ws_maintenance
# Optional override; leave blank to use the database from the URI above.
MONGO_DB_NAME=ws_maintenance
MONGO_USERS_COLLECTION=users
MONGO_WP_COLLECTION=wordpress_sites
MONGO_SUPPORTPAL_COLLECTION=supportpal_sites

